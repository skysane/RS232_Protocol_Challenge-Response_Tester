# Specification: Timeout Configuration

## Overview
Standardize the interpretation of timeout settings to seconds.

## Requirements
- The UI shall display "Timeout (seconds)".
- The backend shall pass the timeout value directly to the serial configuration as seconds.
