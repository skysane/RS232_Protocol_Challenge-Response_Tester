## ADDED Requirements

### Requirement: Pre-verification string
The system SHALL allow users to define a custom string that is transmitted immediately before the verification data.

#### Scenario: Transmitting pre-verification string
- **WHEN** verification process starts
- **THEN** system transmits the pre-defined string before sending the actual verification data
