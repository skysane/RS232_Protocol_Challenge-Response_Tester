## 1. UI and Config Updates

- [x] 1.1 Update `main_gui.py` to change default timeout to 5.0 seconds
- [x] 1.2 Reposition `log_area` in `main_gui.py` to the bottom of the UI

## 2. Logic Flow Implementation

- [x] 2.1 Refactor `main_gui.py` send_command flow to separate data processing from automatic response sending
- [x] 2.2 Add a new UI trigger for users to confirm and transmit verified response data
- [x] 2.3 Ensure comprehensive logging of all traffic flows at each stage
