## ADDED Requirements

### Requirement: Default Timeout Adjustment
The application SHALL default the serial connection timeout to 5.0 seconds.

#### Scenario: Verify initial default
- **WHEN** the application is launched
- **THEN** the timeout entry field displays "5.0"
