## ADDED Requirements

### Requirement: Transmission Control
The system SHALL provide a manual trigger to initiate transmission after verification setup is completed.

#### Scenario: Initiate transmission
- **WHEN** verification setup is complete and user clicks "Transmit"
- **THEN** system transmits the prepared verification data packet
